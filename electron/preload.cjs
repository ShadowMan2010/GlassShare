// Electron Preload Script
// Sets up secure IPC communication channel if required in the future

window.addEventListener('DOMContentLoaded', () => {
  console.log('GlassShare native context bridge initialized.');
});
